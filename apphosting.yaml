# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# Defines the configuration for the runtime environment.
runConfig:
  # The number of CPU cores to allocate to each instance.
  cpu: 1
  # The amount of memory to allocate to each instance.
  memoryMiB: 512
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 2

# It is best practice to manage secrets in Secret Manager.
# https://firebase.google.com/docs/app-hosting/configure-backend#secret-manager
# You can then grant your App Hosting backend access to these secrets and
# reference them in your apphosting.yaml.
# https://firebase.google.com/docs/app-hosting/configure-backend#reference-secrets
env:
  - variable: NEXT_PUBLIC_FIREBASE_API_KEY
    secret: FIREBASE_API_KEY
  - variable: NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
    secret: FIREBASE_AUTH_DOMAIN
  - variable: NEXT_PUBLIC_FIREBASE_PROJECT_ID
    secret: FIREBASE_PROJECT_ID
  - variable: NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
    secret: FIREBASE_STORAGE_BUCKET
  - variable: NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
    secret: FIREBASE_MESSAGING_SENDER_ID
  - variable: NEXT_PUBLIC_FIREBASE_APP_ID
    secret: FIREBASE_APP_ID
